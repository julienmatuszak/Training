/*let len = 5;
let i;

i = i ? i < 0 ? Math.max(0, len + i) : i : 0;
//is i defined, if so, is i negative, if so, find the maximum between 0 and the length of something + i,
//if i is positive, then return i, if i is undefined return 0
*/

